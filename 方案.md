# 拼音学习工具 — 技术方案文档

为即将上小学的男孩打造一款在 iPad 上运行的图形化汉语拼音学习工具。

---

## 1. 项目定位

- **使用者**：幼升小阶段的 6~7 岁男孩
- **运行设备**：iPad（触控为主），同时兼容手机/桌面浏览器
- **形态**：网页应用（PWA，`Progressive Web App`），用 Safari「添加到主屏幕」即可全屏运行，无需 App Store
- **核心目标**：每天通过有趣的方式学习、掌握、巩固汉语拼音（声母 / 韵母 / 整体认读 / 拼读 / 声调）

---

## 2. 技术选型（路线 A — PWA 网页应用）

| 项 | 选型 | 理由 |
|----|------|------|
| 语言 | **原生 HTML / CSS / JavaScript**（无框架） | 零依赖、加载快、iPad Safari 兼容最佳、无构建步骤、直接用浏览器打开 |
| 发音 | **Web Speech API (`speechSynthesis`)** | iOS 自带中文 TTS，离线可用、零成本、无需录音 63 个音素 |
| 存储 | **localStorage** | 记录打卡/进度/错题，无需后端数据库 |
| PWA | `manifest.json` + service worker | 可「添加到主屏幕」全屏运行、可离线 |
| 音频细节 | iOS `speechSynthesis` 保证中文语音「完整朗读不带标点」需额外处理 | 见 §7 发音方案 |

> **为什么不用原生 App**:无需 $99 开发者账号、无 App Store 审核、开发快、家人朋友点链接即可用；后续如需原生可平滑迁移。

---

## 3. 功能模块设计（完整版）

### 模块总览

| 模块 | 说明 | MVP 优先级 |
|------|------|-----------|
| 🎯 **学习**（声母/韵母/整体认读） | 卡片式学习，点卡片发音、看例字 | P0 |
| 🔊 **拼读练习** | 声母+韵母组合拼读，对照例字 | P0 |
| 🎮 **游戏化练习** | 听音选拼音、看拼音选字、看字拼拼音 | P0 |
| ⭐ **声调练习** | 四声拖拽/点选 | P1 |
| ✅ **每日打卡** | 完成任务打卡、日历记录 | P1 |
| 🏆 **家长模式** | 看进度、正确率、错题本、设置每日目标 | P1 |
| 🎨 **奖励动画** | 星星/奖杯/表扬语 | P1 |

### 3.1 拼音知识库（核心数据）

**声母（23个）**：b p m f d t n l g k h j q x zh ch sh r z c s y w

**韵母（24个）**：a o e i u ü ai ei ui ao ou iu ie üe er an en in un ün ang eng ing ong

**整体认读音节（16个）**：zhi chi shi ri zi ci si yi wu yu ye yue yuan yin yun ying

**四声标记**：ā á ǎ à / ō ó ǒ ò / ē é ě è / ī í ǐ ì / ū ú ǔ ù / ǖ ǘ ǚ ǜ

数据用完整 JSON 结构内置在 `js/pinyin-data.js`，含发音字段（统一以无调号的拼音+数字声调喂给 TTS，保证 iOS 发音准确）。

### 3.2 界面布局（iPad 触控优化）

- 顶栏：标题 + 当前所处模块 + 打卡状态
- 主区：卡片网格 / 练习区
- 底部：模块切换导航（学习 / 练习 / 打卡 / 家长）
- 全部为大按钮（≥88pt 触控目标）、大字体、高对比彩色卡通风格

---

## 4. 游戏化练习设计

### 4.1 听音选拼音（四选一）
1. TTS 朗读一个拼音（如读 "bā"）
2. 屏幕出现 4 个拼音选项
3. 孩子点击正确的 → 奖励 + 动画；错误 → 提示并标红
4. 进入错题本

### 4.2 看拼音选汉字
1. 显示拼音（带声调），配 4 个候选汉字
2. 孩子选出发音正确的汉字

### 4.3 看字拼拼音
1. 显示一个汉字
2. 从声母+韵母+声调中组装正确拼音

### 奖励机制
- 每次答对 +星⭐
- 连续答对连击（combo）额外加分
- 集满 10 星解锁一枚奖杯 🏆
- 每日达标后庆祝动画

---

## 5. 打卡与家长报告

- **打卡**：每日完成一组练习（如正确 10 题）即可打卡，日历上点亮当天
- **家长模式**（简单 PIN 或手势进入）：
  - 连续打卡天数
  - 各模块正确率
  - **错题本**（薄弱音节排行）
  - 设置每日目标题数
  - 数据存 localStorage，可导出 JSON

---

## 6. PWA 配置与 iPad 部署

### manifest.json 核心字段
```json
{
  "name": "拼音小达人",
  "short_name": "拼音",
  "display": "standalone",
  "start_url": "/",
  "background_color": "#FFF3E0",
  "theme_color": "#FF9800",
  "icons": [{ "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" }]
}
```

### 添加到主屏幕（iPad Safari）
1. 打开网页 → 点击 Safari 分享按钮（方框上箭头）
2. 选择「添加到主屏幕 Add to Home Screen」
3. 桌面出现图标，点击即全屏运行（`display: standalone` 隐藏浏览器地址栏）

### service worker（可离线）
缓存 HTML/CSS/JS/拼音数据，联网一次后离线可用（TTS 在 iOS 上离线可发音）。

---

## 7. 发音方案（关键）

### 7.1 iOS 自带中文 TTS
```js
function speak(pinyinWithTone, lang = "zh-CN") {
  const u = new SpeechSynthesisUtterance(pinyinWithTone);
  u.lang = lang;
  // 选择中文语音
  const voices = speechSynthesis.getVoices();
  const zh = voices.find(v => v.lang.startsWith("zh"));
  if (zh) u.voice = zh;
  u.rate = 0.8; // 放慢语速，适合儿童
  speechSynthesis.speak(u);
}
```

### 7.2 iOS 发音坑与规避
- **坑1**：iOS TTS 会把「ü」读不准，且带声调字符（āáǎà）在某些 iOS 版本读成字母。**规避**：喂给 TTS 的文本用「无调号拼音 + 数字声调」（如 `ba1`→`bā`），或直接用带声调字符串测试微调。
- **坑2**：iOS 首段音频偶尔丢失。**规避**：先初始化一次空 utterance 唤醒语音引擎，或用 `speechSynthesis.cancel()` + 短暂延时。
- **坑3**：多个快速朗读会叠加。**规避**：每次 `speechSynthesis.cancel()` 后再 `speak`。
- **备选**：若 TTS 发音不理想，后续可替换为真人录音（63 个音素短音频），架构上预留 `audio/` 目录，`speak()` 先找录音文件、找不到再 fallback TTS。

### 7.3 读音数据策略
- 每个拼音条目存 `{ pinyin: "ba", tone: 1, text: "八", sound: "ba1" }`
- `sound` 字段是喂给 TTS 的字符串（`ba1`、`ao3`…），绕开 iOS 对带调字符的误读

---

## 8. 目录结构

```
pinyin_tool/
├── index.html              # 入口，单页应用
├── manifest.json           # PWA 配置
├── sw.js                   # service worker（离线缓存）
├── css/
│   └── style.css           # 全局样式（卡通/大按钮/iPad 适配）
├── js/
│   ├── pinyin-data.js      # 拼音知识库（声母/韵母/整体认读/声调/例字）
│   ├── tts.js              # 发音封装（waits语音唤醒/规避）
│   ├── app.js              # 路由 + 状态 + 打卡逻辑
│   ├── learn.js            # 学习模块
│   ├── practice.js         # 游戏化练习模块
│   ├── checkin.js          # 打卡模块
│   └── parents.js          # 家长模式（报告/错题/设置）
├── icons/
│   └── icon-192.png        # App 图标
└── README.md
```

---

## 9. 分阶段实施计划

| 阶段 | 内容 | 产出 |
|------|------|------|
| **P0（MVP）** | 声母/韵母/整体认读学习卡 + 拼读 + TTS 发音 | 可学可听的网页 |
| **P1** | 游戏化练习（听音选拼音等）+ 声调练习 | 可玩 |
| **P2** | 打卡 + 家长报告 + 错题本 | 完整闭环 |
| **P3** | PWA 打包（manifest + sw）+ iPad 图标 + 部署 | 一键全屏可用 |

---

## 10. 部署方式（供家人朋友使用）

- **最简单**：GitHub Pages / Netlify / Vercel 托管，获得一个 https 链接，分享到微信或 Safari 打开就能用
- **局域网**：若仅家庭内部，可用 iPad Safari 访问局域网内 web 服务
- 因为是纯前端 + 无后端，部署零成本

---

*文档版本 v1.0 · 2026-08* 
